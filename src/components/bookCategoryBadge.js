import { Link } from "gatsby"
import PropTypes from "prop-types"
import React from "react"

const getCategoryName = () => {
    switch(categoryId){
        case 1:
        case 2:
        default:
            ""
}

const bookCategoryBadge = ({categoryId}) => (
    <div>
        
    </div>
)

export default bookCategoryBadge;